---
title: "Verified Multi-Agent Orchestration: A Plan-Execute-Verify-Replan Framework for Complex Query Resolution"
title_zh: 验证式多智能体编排：一种用于复杂查询解析的“规划-执行-验证-重规划”框架
authors: "Xing Zhang, Yanwei Cui, Guanghui Wang, Qucy Wei Qiu, Ziyuan Li, Fangwei Han, Yajing Huang, Hengzhi Qiu, Bin Zhu, Peiyang He"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11445v1"
tags: ["query:llm-rs"]
score: 10.0
evidence: 通过验证循环协调专业LLM智能体的框架
tldr: 本研究提出VMAO框架，通过“规划-执行-验证-重规划”循环协调多智能体解决复杂查询。该框架将任务分解为有向无环图（DAG）并并行执行，利用LLM验证器评估结果完整性，并根据反馈动态调整计划。实验表明，VMAO在市场调研任务中显著提升了答案完整性和来源质量，证明了编排级验证在多智能体质量保证中的有效性。
selection_source: fresh_fetch
motivation: 针对复杂查询，现有的单智能体或简单多智能体系统在处理深度任务时难以保证结果的完整性和信息来源的可靠性。
method: 提出一种基于DAG任务分解、并行执行、LLM验证及自适应重规划的闭环多智能体编排框架。
result: 在专家策划的市场调研测试中，该框架将答案完整性评分从3.1提升至4.2，来源质量评分从2.6提升至4.1。
conclusion: 编排层面的验证机制是提升多智能体系统输出质量和可靠性的关键，能有效平衡答案质量与资源消耗。
---

## 摘要
我们提出了验证式多智能体编排（VMAO），这是一个通过验证驱动的迭代循环来协调基于大语言模型（LLM）的专业智能体的框架。针对复杂查询，我们的系统将其分解为子问题的有向无环图（DAG），通过特定领域的智能体并行执行，利用基于 LLM 的评估来验证结果的完整性，并进行自适应重规划以弥补差距。主要贡献包括：(1) 在子问题 DAG 上实现具有自动上下文传播的依赖感知并行执行；(2) 采用基于 LLM 的验证器作为编排级协调信号的验证驱动自适应重规划；(3) 可配置的停止条件，用于平衡回答质量与资源消耗。在 25 个专家策划的市场研究查询中，与单智能体基准相比，VMAO 将回答完整性从 3.1 提升至 4.2，来源质量从 2.6 提升至 4.1（1-5 分制），证明了编排级验证是多智能体质量保证的有效机制。

## Abstract
We present Verified Multi-Agent Orchestration (VMAO), a framework that coordinates specialized LLM-based agents through a verification-driven iterative loop. Given a complex query, our system decomposes it into a directed acyclic graph (DAG) of sub-questions, executes them through domain-specific agents in parallel, verifies result completeness via LLM-based evaluation, and adaptively replans to address gaps. The key contributions are: (1) dependency-aware parallel execution over a DAG of sub-questions with automatic context propagation, (2) verification-driven adaptive replanning that uses an LLM-based verifier as an orchestration-level coordination signal, and (3) configurable stop conditions that balance answer quality against resource usage. On 25 expert-curated market research queries, VMAO improves answer completeness from 3.1 to 4.2 and source quality from 2.6 to 4.1 (1-5 scale) compared to a single-agent baseline, demonstrating that orchestration-level verification is an effective mechanism for multi-agent quality assurance.

---

## 论文详细总结（自动生成）

以下是对论文《Verified Multi-Agent Orchestration: A Plan-Execute-Verify-Replan Framework for Complex Query Resolution》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对的是**复杂查询解析（Complex Query Resolution）**中的多智能体协作问题。在市场调研等领域，查询往往需要跨学科知识、多源数据整合及矛盾消除。
*   **现有挑战**：现有的多智能体框架（如 AutoGen, MetaGPT）虽然提供了灵活的交互模式，但缺乏**编排层面的质量验证**和**自适应调整机制**。单智能体系统在处理深度任务时，难以保证结果的完整性和来源的可靠性。
*   **核心目标**：建立一个能够自动分解任务、并行执行、自我验证并根据反馈动态调整计划的闭环框架，以减少人工干预并提高输出质量。

### 2. 方法论：核心思想与算法流程
VMAO 框架的核心思想是**“规划-执行-验证-重规划”的迭代闭环**。
*   **五阶段流程**：
    1.  **Plan（规划）**：将复杂查询分解为子问题的**有向无环图（DAG）**。每个节点包含子问题、指定的智能体类型、依赖关系和验证标准。
    2.  **Execute（执行）**：根据 DAG 的依赖关系并行执行任务。支持上下文传播（上游结果作为下游输入）。
    3.  **Verify（验证）**：由独立的 LLM 验证器评估每个子问题的结果，输出完整性评分（0-1）、缺失点及处理建议（接受/重试/升级）。
    4.  **Replan（重规划）**：针对验证中的缺口，决定是重试失败任务、引入新查询还是合并现有结果。
    5.  **Synthesize（综合）**：当满足停止条件时，进行分层汇总，生成带引用的最终报告。
*   **关键技术细节**：
    *   **智能体分类法**：分为三层（Tier 1: 数据采集；Tier 2: 分析推理；Tier 3: 结果输出）。
    *   **停止条件**：引入了 5 种可配置条件（完整性阈值 >80%、高置信度、收益递减 <5%、Token 预算 1M、最大迭代次数 3）。
    *   **工具集成**：通过模型上下文协议（MCP）连接了 42 个专业工具。

### 3. 实验设计
*   **场景与数据集**：使用了 **25 个由专家策划的市场调研查询**，涵盖性能分析、竞争情报、财务调查和战略评估四个类别。
*   **Benchmark（基准）**：
    *   **Single-Agent**：具备所有工具访问权限的单个推理智能体。
    *   **Static Pipeline**：预定义的智能体顺序执行序列（RAG -> Web -> 财务 -> 分析 -> 综合），无验证和重规划。
*   **评估指标**：采用 1-5 分制评估 **Completeness（完整性）** 和 **Source Quality（来源质量）**。评估由 Claude Opus 4.5 初评，再由人类专家审核调整。

### 4. 资源与算力
*   **模型使用**：主要基于 **AWS Bedrock** 平台。
    *   执行模型：Claude Sonnet 4.5（主模型），Claude Haiku 4.5（回退模型）。
    *   验证与评估模型：Claude Opus 4.5（提供独立的高质量信号）。
*   **算力说明**：文中**未明确提及具体的 GPU 型号或数量**。由于是基于 API 的应用层研究，重点在于 Token 消耗。VMAO 平均每个查询消耗约 **850K tokens**，耗时约 **900 秒**。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 25 组复杂查询实验。虽然样本数量（N=25）在统计学上不算巨大，但考虑到每个查询的复杂性（涉及 8-12 个子问题、50万-110万 Token 消耗、10-20 分钟执行时间），实验具有一定的深度。
*   **充分性与公平性**：
    *   对比了单智能体和静态流水线，展示了闭环系统的增量价值。
    *   使用了不同于执行模型的更强模型（Opus）进行评估，以减少自我偏见。
    *   **不足之处**：缺乏组件级的消融实验（例如：单独测试 DAG 的贡献或单独测试重规划的贡献）。

### 6. 主要结论与发现
*   **性能提升**：VMAO 在完整性（4.2 vs 3.1）和来源质量（4.1 vs 2.6）上显著优于单智能体基准。
*   **验证的价值**：在**战略评估（Strategic Assessment）**等开放式、多维度查询中提升最明显（完整性提升 53%），因为这类任务初始规划最容易遗漏信息。
*   **重规划行为**：大多数重规划动作是“重试”而非“新增”，说明智能体执行的波动性（工具调用失败等）是导致信息缺口的主要原因。

### 7. 优点
*   **闭环可靠性**：通过编排层面的验证，解决了多智能体系统输出不可控的痛点。
*   **高效并行**：基于 DAG 的调度在保证依赖关系的同时最大化了执行效率。
*   **高度可配置**：提供了详细的停止条件和智能体分类，便于在成本和质量之间做权衡。
*   **透明度**：DAG 结构使得复杂的推理过程变得可解释、可追踪。

### 8. 不足与局限
*   **成本高昂**：VMAO 的 Token 消耗是单智能体的 **8.5 倍**，不适合对成本或延迟极度敏感的场景。
*   **验证局限**：验证器主要评估“完整性”而非“绝对事实准确性”，仍可能存在无法识别的幻觉。
*   **模型偏差**：所有实验均在 Claude 系列模型上完成，框架在其他模型家族（如 GPT-4, Llama 3）上的泛化能力尚未验证。
*   **评估样本量**：25 个查询的样本量较小，且未报告置信区间。

（完）
